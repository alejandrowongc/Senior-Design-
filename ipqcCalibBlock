#!/usr/bin/env python3
import cv2
import numpy as np
from picamera2 import Picamera2
import time

# ---------------- Camera Setup ----------------
picam2 = Picamera2()
config = picam2.create_preview_configuration(main={"size": (800, 480)})
picam2.configure(config)
picam2.start()
time.sleep(2)

# Lock exposure and white balance
controls = {"AwbEnable": False, "AeEnable": False}
picam2.set_controls(controls)
picam2.set_controls({"AwbMode": 0, "ColourGains": (1.8, 1.0)})

# ROI (main measurement zone)
ROI_X1, ROI_Y1, ROI_X2, ROI_Y2 = 10, 175, 790, 325

# ROI2 (high accuracy vertical band, 30px wide, centered in frame)
ROI2_WIDTH = 30
FRAME_WIDTH = 800
roi2_center_x = FRAME_WIDTH // 2

ROI2_X1 = roi2_center_x - ROI2_WIDTH // 2
ROI2_X2 = roi2_center_x + ROI2_WIDTH // 2
ROI2_Y1 = ROI_Y1
ROI2_Y2 = ROI_Y2

PIXELS_PER_INCH = 42.0
TARGET_LENGTH = 3.0
TOL = 0.125

LOW_LIMIT = TARGET_LENGTH - TOL
HIGH_LIMIT = TARGET_LENGTH + TOL

print("Press 'q' to quit")

while True:
    frame = picam2.capture_array()
    frame_rgb = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)

    # Draw ROI and ROI2
    cv2.rectangle(frame_rgb, (ROI_X1, ROI_Y1), (ROI_X2, ROI_Y2), (255, 0, 0), 2)
    cv2.rectangle(frame_rgb, (ROI2_X1, ROI2_Y1), (ROI2_X2, ROI2_Y2), (0, 255, 255), 2)

    # Extract ROI
    roi = frame_rgb[ROI_Y1:ROI_Y2, ROI_X1:ROI_X2]

    # Preprocessing
    gray = cv2.cvtColor(roi, cv2.COLOR_RGB2GRAY)
    blur = cv2.GaussianBlur(gray, (5, 5), 0)
    edges = cv2.Canny(blur, 50, 150)
    kernel = np.ones((3,3), np.uint8)
    edges = cv2.morphologyEx(edges, cv2.MORPH_CLOSE, kernel)

    contours, _ = cv2.findContours(edges, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)

    for cnt in contours:
        area = cv2.contourArea(cnt)
        if area < 1000 or area > 15000:
            continue

        x, y, w, h = cv2.boundingRect(cnt)

        # Length measurement
        length_pixels = max(w, h)
        length_inches = length_pixels / PIXELS_PER_INCH

        # PASS/REJECT logic
        if LOW_LIMIT <= length_inches <= HIGH_LIMIT:
            result = "PASS"
            color = (0, 255, 0)
        else:
            result = "REJECT"
            color = (0, 0, 255)

        # Draw bounding box always
        cv2.rectangle(roi, (x, y), (x + w, y + h), color, 2)

        # Compute centroid
        cx, cy = x + w // 2, y + h // 2
        cv2.circle(roi, (cx, cy), 3, (255, 255, 0), -1)

        # Convert centroid to full-frame coordinates
        cx_global = cx + ROI_X1
        cy_global = cy + ROI_Y1

        # Only display result when midpoint enters ROI2
        if ROI2_X1 <= cx_global <= ROI2_X2:
            cv2.putText(
                roi,
                f"{length_inches:.3f} in",
                (x, y - 10),
                cv2.FONT_HERSHEY_SIMPLEX,
                0.6,
                (255, 255, 255),
                2,
            )
            cv2.putText(
                roi,
                result,
                (x, y + h + 20),
                cv2.FONT_HERSHEY_SIMPLEX,
                0.7,
                color,
                2,
            )

    cv2.imshow("Full Frame (ROI Marked)", frame_rgb)

    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cv2.destroyAllWindows()
picam2.stop()
